Simply Sans Bold is bundled here as CustomFont.ttf and is the default
font for this addon. It's licensed under the SIL Open Font License,
included as SimplySans-LICENSE.txt, which permits bundling with software.

To use your own font instead, replace CustomFont.ttf with any .ttf file
you own the rights to (keep the exact filename "CustomFont.ttf"), then
in the addon's Settings panel (gear icon, or /xqc options) make sure
"Simply Sans Bold" is selected in the Font dropdown -- that entry now
points at whatever file is in this folder. Reload UI (/reload) if a
font change doesn't apply immediately.

If the file is ever missing or invalid, the addon automatically falls
back to the default stock Friz Quadrata font.
